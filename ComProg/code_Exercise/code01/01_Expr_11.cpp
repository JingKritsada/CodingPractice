#include <bits/stdc++.h>
using namespace std;

int main()
{
    double num1 = M_PI - ((3628800.00) / pow(8, 8));
    double num2 = pow((log(9.7)), ((7.0 / sqrt(71)) - sin(2.0 / 9.0 * M_PI)));
    double num3 = pow(1.2, pow(2.3, 1.0 / 3.0));

    cout << (num1 + num2) / num3;

    return 0;
}